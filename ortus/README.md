Instruction on running server:
sh setup.sh
pip install -r requirements.txt

Above commands will set up necessary dependencies on your system

On two separate terminal:
node server.js
python manage.py runserver


